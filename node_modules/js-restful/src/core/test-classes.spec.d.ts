import { ISecurityContext } from './descriptions';
export declare class Book {
    id: number;
    name: string;
}
export declare class BookService {
    testProperty: number;
    unDecoratedMethod(): void;
    allBooks(): Book[];
    createBook(name: string, token: string): Book;
    updateBook(id: number, name: string): Book;
    deleteBook(id: number, time: number): boolean;
    contextTest(req: any, res: any): boolean;
    securityContextTest(context: ISecurityContext): boolean;
}
